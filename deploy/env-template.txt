# ── Backend environment variables ────────────────────────────────────────────
GEMINI_API_KEY=          # Google AI Studio or Vertex AI API key
GEMINI_MODEL=gemini-live-2.5-flash-native-audio  # or latest available live model
MOCK_MODE=false          # Set true for demo without API key
ALLOWED_ORIGINS=https://your-frontend-domain.run.app
GCP_PROJECT_ID=          # Google Cloud project ID

# ── Frontend environment variables ───────────────────────────────────────────
NEXT_PUBLIC_WS_URL=wss://your-backend-domain.run.app/ws
NEXT_PUBLIC_APP_NAME=Logos

# ── Deployment variables ──────────────────────────────────────────────────────
GCP_REGION=us-central1
BACKEND_URL=https://logos-backend-XXXX-uc.a.run.app
